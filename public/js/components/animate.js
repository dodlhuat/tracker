export let animate = {
    element: (animation, element) => {
        // todo: animate element
        /*
        remove all animate classes from that one element and add the new one
         */
    }
}
